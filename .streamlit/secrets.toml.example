# Streamlit Secrets Configuration Example
# Copy this file to .streamlit/secrets.toml and add your API keys
# For Streamlit Cloud: Add secrets via the dashboard (Settings > Secrets)

# EVDS API Key (if needed for future data updates)
# EVDS_API_KEY = "your_evds_api_key_here"

# Note: The app primarily uses pre-processed data files
# API keys are only needed if you plan to update data via the app
